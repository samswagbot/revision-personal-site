import React from 'react';
import { Landing, MainContainer } from '../components/components';

const HomePage = () => (
  <MainContainer>
    <Landing />
  </MainContainer>
);

export default HomePage;
